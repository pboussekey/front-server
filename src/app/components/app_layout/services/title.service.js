angular.module('app_layout')
    .factory('title_service',['$timeout',
        function($timeout){
            var service = {
                
            };
            return service;
        }
    ]);
